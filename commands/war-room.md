---
description: Convene the war-room on a hard decision — get the winning strategy and how it loses
argument-hint: "<the decision to settle>"
---

Convene the **war-room** on this decision:

$ARGUMENTS

Run the `war-room` skill. Frame the war, convene the five officers as non-colluding subagents (the Scout, the Strategist, the Marshal, the Quartermaster, and — last, against the emerging consensus — the Tenth Man), assemble the dossier (`STRATEGY` / `BATTLE-PLAN` / `HOW-WE-LOSE`), validate its structure with `hooks/validate-dossier.sh`, and present it to me as the Commander to seal.

If a named oracle (a test, a spec, an invariant) could settle this instead, say so and stop — that is a verification, not a war.
